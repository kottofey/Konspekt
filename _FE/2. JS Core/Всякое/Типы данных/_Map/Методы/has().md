Проверяем через метод `.has()`
```js
const map = new Map();

map.set(42, 'Ответ на главный вопрос жизни, вселенной и всего такого');
map.set('42', 'Ответ на главный вопрос жизни, вселенной и всего такого');

console.log(map.has(42)); // true
console.log(map.has(43)); // false
```
