Смотрим через метод `.size()`

```js
const map = new Map();

map.set(42, 'Ответ на главный вопрос жизни, вселенной и всего такого');
map.set('42', 'Ответ на главный вопрос жизни, вселенной и всего такого');

console.log(map.size); // 2
```
